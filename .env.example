# Copy to .env.local for local shell use.
# Never commit .env.local.
#
# Wrangler reads CLOUDFLARE_API_TOKEN from the shell environment for deploys.
# Keep the token in your shell profile, password manager, or ignored .env.local.
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
